name: self_finance
description: It is a finance data storing app. Which help the user to save there coustomer details and calculate the intrest in real time, It is an offline app so the data will be safe inside the device , this app can run in windows, Andriod and IOS.

# publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 2.0.2+4

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_launcher_icons: ^0.13.1
  flutter_svg: ^2.0.10+1
  responsive_sizer: ^3.3.1
  url_launcher: ^6.2.6
  pinput: ^4.0.0
  sqflite: ^2.3.3+1
  path_provider: ^2.1.3
  local_auth: ^2.2.0
  flutter_svg_provider: ^1.0.7
  intl: ^0.19.0
  fl_chart: ^0.68.0
  image_picker: ^1.1.1
  photo_view: ^0.15.0
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  liquid_pull_to_refresh: ^3.0.1
  currency_picker: ^2.0.21
  screenshot: ^3.0.0
  share_plus: ^9.0.0
  feedback: ^3.1.0
  flutter_email_sender: ^6.0.3
  google_mobile_ads: ^5.1.0
  firebase_core: ^2.31.0
  firebase_analytics: ^10.10.5
  firebase_performance: ^0.9.4+5
  firebase_auth: ^4.19.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.7
  custom_lint: ^0.6.4
  riverpod_lint: ^2.3.10
  analyzer: ^6.4.1

analyzer:
  plugins:
    - custom_lint

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/logo.png"
  adaptive_icon_background: "#000"
  adaptive_icon_foreground: "assets/icon/logo.png"
  remove_alpha_ios: true
  min_sdk_android: 21

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/fonts/
    - assets/icon/

  fonts:
    - family: hell
      fonts:
        - asset: assets/fonts/Helvetica.ttf
          style: normal
        - asset: assets/fonts/Helvetica-Oblique.ttf
          style: italic
        - asset: assets/fonts/Helvetica-Bold.ttf
          weight: 700
        - asset: assets/fonts/helvetica-light.ttf
          weight: 300
        - asset: assets/fonts/Helvetica-BoldOblique.ttf
          weight: 700
          style: italic
